/* eslint-disable no-console */

// standard java script code that supports ES5
// var chalk = require("chalk")

// Babel code, use latest version of javascript code ES6 and also it converts/compiles/transpiles
// the latest java script(ES6) code to Standard Javascript(ES5) support
import chalk from 'chalk'

console.log(chalk.green('Starting app in dev mode'));
